#!/usr/bin/env bash
sudo apt-get update
sudo apt -y install \
    stow \

for dir in *; do
    if [ -d "$dir" ]; then
        $dir/init
        # stow --ignore=init $dir
        stow $dir
    fi
done
