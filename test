#!/usr/bin/env bash
sudo apt-get update
sudo apt-get -y install \
    stow \
    wget

mkdir -p ~/.local/bin && source ~/.profile

for dir in *; do
    if [ -d "$dir" ]; then
        $dir/init
        # stow --ignore=init $dir
        stow $dir
    fi
done
